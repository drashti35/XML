<?xml version="1.0"?>
	<!DOCTYPE details[
		<!ELEMENT details (Email)>
		<!ELEMENT Email (message)>
		<!ELEMENT message (#PCDATA)>
		<!ATTLIST message from CDATA #REQUIRED>
		<!ATTLIST message to CDATA #REQUIRED>
		<!ATTLIST message subject CDATA #REQUIRED>
		]>
	<details>
		<Email>
			<message from="abc" to="xyz" subject="greeting">Hello World</message>
		</Email>
	</details>
	<!--Create an XML document email where it contain the following data:
Email(message) as an element and Message(from, to, subject)as
an attribute.
Create a DTD for the following XML document.-->